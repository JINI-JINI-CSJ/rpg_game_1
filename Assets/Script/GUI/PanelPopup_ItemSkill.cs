using UnityEngine;
using UnityEngine.UI;

public class PanelPopup_ItemSkill : MonoBehaviour
{
    public SJ_UIGridMove_PlayerInput    uiGridMove_PlayerInput;

    public GameObject   go_DESC;
    public Text         text_Desc;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // 
    static public void Open_CharSkill( CharBase charBase , SJ_COMMON.Func_Arg func_ok )
    {
        
    }


    public void OnMoveItem( object obj )
    {
        

    }
}
