using UnityEngine;

// 미니맵을 위한 클래스
// 
public class SJ_MapTileViewer_Minimap : SJ_MapTileViewer
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
