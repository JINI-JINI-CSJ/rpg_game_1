using UnityEngine;

// 타일 위에 보일 3디 객체
// 이동가능 여부
// 상호 작용 : 포털 , 계단 , 석상 , 랜드마크 등등
public class TileResObj : MonoBehaviour
{
    // 이동가능 여부
    public bool moveAble;

    // 플레이어 앞에서 조사 가능 여부
    public bool frontResearch; 

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
